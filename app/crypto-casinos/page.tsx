import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function Page() {
  return (
    <PagePlaceholder
      kicker="Crypto casinos"
      title="Crypto casino index"
      standfirst="All 47 operators, ranked and filterable — no-KYC, fastest-payout, lowest-wagering, sportsbook and esports cuts live on their own URLs below."
    />
  );
}
