import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function Page() {
  return (
    <PagePlaceholder
      kicker="Slots · High volatility"
      title="High volatility slots"
      standfirst="Bigger, rarer wins for players who can withstand longer dry spells."
    />
  );
}
