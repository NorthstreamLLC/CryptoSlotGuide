import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function Page() {
  return (
    <PagePlaceholder
      kicker="Compare"
      title="Compare builder"
      standfirst="Pick 2–4 operators and compare them head to head."
    />
  );
}
