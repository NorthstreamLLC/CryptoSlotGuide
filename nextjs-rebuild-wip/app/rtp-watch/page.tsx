import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function Page() {
  return (
    <PagePlaceholder
      kicker="RTP Watch"
      title="RTP Watch"
      standfirst="The slot × operator RTP matrix — the site's differentiator."
    />
  );
}
